[package]
name = "mui-leptos-example"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos.workspace = true
mui-system = { path = "../../crates/rustic-ui-system", features = ["leptos"] }
tokio = { workspace = true, optional = true }

[features]
default = ["csr"]
csr = []
ssr = ["tokio"]
