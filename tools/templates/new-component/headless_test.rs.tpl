//! Headless state machine smoke-test placeholder for `{{component_pascal}}`.
//!
//! Use this file to capture boundary conditions once the state machine is
//! implemented. The generated assertion keeps automation identifiers in lockstep
//! with the docs stub and TypeScript adapter until real coverage lands.

#[test]
#[ignore = "Replace with real {{component_pascal}} state machine assertions"]
fn {{component_snake}}_headless_state_preserves_default_automation_root() {
    let automation_root = "{{automation_id}}";
    assert_eq!(automation_root, "{{automation_id}}", "update docs + adapters if the automation root changes");
}
